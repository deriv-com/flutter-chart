name: showcase_app
description: A showcase app for the Deriv Chart package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  deriv_chart:
    path: ../
  provider: ^6.0.5
  flutter_localizations:
    sdk: flutter
  pref: ^2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
