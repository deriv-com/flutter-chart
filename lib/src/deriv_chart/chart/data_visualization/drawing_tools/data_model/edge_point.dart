/// A class that holds epoch and yCoord of the edge points.
class EdgePoint {
  /// Initializes
  const EdgePoint({
    this.epoch = 0,
    this.quote = 0,
  });

  /// Epoch.
  final int epoch;

  /// Quote.
  final double quote;
}
